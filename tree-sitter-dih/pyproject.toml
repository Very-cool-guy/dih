[build-system]
build-backend = "setuptools.build_meta"
requires = [ "setuptools>=62.4.0", "wheel" ]

[project]
authors = [ { name = "Lucas" } ]
classifiers = [
  "Intended Audience :: Developers",
  "Topic :: Software Development :: Compilers",
  "Topic :: Text Processing :: Linguistic",
  "Typing :: Typed",
]
description = "Dih grammar for tree-sitter"
keywords = [ "incremental", "parsing", "tree-sitter", "dih" ]
license.text = "MIT"
name = "tree-sitter-dih"
readme = "README.md"
requires-python = ">=3.10"
version = "0.0.1"

[project.urls]
Homepage = "https://github.com/Very-cool-guy/dih"

[project.optional-dependencies]
core = [ "tree-sitter~=0.24" ]

[tool.cibuildwheel]
build = "cp310-*"
build-frontend = "build"
